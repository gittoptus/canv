[phases.setup]
nixPkgs = ["bun", "nodejs_20"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["touch .env.local && bun run build"]

[start]
cmd = "touch .env.local && bun run db:push && cd apps/nextjs && bun run start"
